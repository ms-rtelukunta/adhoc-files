parameters:
- name: stageName
  type: string
  default: Build

stages:
- stage: ${{ parameters.stageName }}
  displayName: 'Build Stage'
  jobs:
  - template: templates/nested/job-template.yaml
    parameters:
      jobName: BuildJob

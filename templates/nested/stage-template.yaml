parameters:
- name: stageName
  type: string
  default: Build

stages:
- stage: ${{ parameters.stageName }}
  displayName: 'Build Stage'
  jobs:
  - template: job-template.yaml
    parameters:
      jobName: BuildJob
